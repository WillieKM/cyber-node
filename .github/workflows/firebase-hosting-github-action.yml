git add .github/workflows/firebase-hosting-github-action.yml
git commit -m "fix: Pushing final verified and clean YAML code"
git push

Please let me know the status of the new workflow run on the **Actions** tab after you run `git push`! It should be green this time.